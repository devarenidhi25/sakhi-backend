uvicorn
fastapi
google-generativeai
python-dotenv
pymongo
transformers==4.40.1
spacy==3.7.4
reportlab
bcrypt
authlib
torch
itsdangerous
authlib[httpx]  # ensures all its optional deps are installed too

# Add this explicitly to avoid wheel build failure
blis==0.7.11

# Optional but safe
numpy==1.26.4
